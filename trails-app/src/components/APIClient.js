/*
import axios from 'axios';

const SEARCH_URL = (zip) => '/api/search?zip=' + zip;
const APIKey = "&key=200964805-fbbd50c01b329d117306d1834dfd6a2d";
const maxDistance = "&maxDistance=20";
const maxResults = "&maxResults=30";

const APIClient = {
    query(zip){
        zip = encodeURIComponent(zip)
    }
}

window.APIClient = APIClient
window.axios = axios;

export default APIClient;
*/